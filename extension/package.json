{
  "name": "atrium-cockpit",
  "displayName": "Atrium Cockpit",
  "description": "Linear / PRD / sprint cockpit for Claude Code \u2014 live mock-up POC running as a VS Code extension.",
  "version": "0.0.26",
  "publisher": "atrium",
  "private": true,
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "https://github.com/stonekey908/atrium.git",
    "directory": "extension"
  },
  "engines": {
    "vscode": "^1.98.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "atrium.openDashboard",
        "title": "Atrium: Open Cockpit",
        "icon": "$(rocket)"
      },
      {
        "command": "atrium.refreshBoard",
        "title": "Atrium: Refresh Board",
        "icon": "$(refresh)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "atrium.refreshBoard",
          "when": "activeWebviewPanelId == atrium.dashboard",
          "group": "navigation"
        }
      ]
    },
    "keybindings": [
      {
        "command": "atrium.openDashboard",
        "key": "ctrl+alt+a",
        "mac": "cmd+alt+a"
      }
    ],
    "configuration": {
      "title": "Atrium",
      "properties": {
        "atrium.openOnStartup": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Open the Atrium cockpit automatically as an editor tab when VS Code finishes starting. Turn off if you'd rather open it on demand via the status-bar button, the `Atrium: Open Cockpit` command, or `\u2318\u2325A`."
        },
        "atrium.linear.apiKey": {
          "type": "string",
          "default": "",
          "scope": "application",
          "markdownDescription": "Linear personal API key. When set, the cockpit pulls the board **live** from Linear on refresh; otherwise it uses the committed snapshot. Create one at [linear.app/settings/api](https://linear.app/settings/api)."
        },
        "atrium.linear.projectName": {
          "type": "string",
          "default": "",
          "markdownDescription": "Linear project the cockpit loads when a live API key is configured. **Empty = auto-detect** from the workspace folder name. Set it (or pick from the header dropdown, which writes it to **workspace** settings) to pin a project for this workspace."
        },
        "atrium.linear.wavePrefix": {
          "type": "string",
          "default": "ATR Wave",
          "markdownDescription": "Label prefix that marks a **wave / sprint**. Any Linear label starting with this (case-insensitive) becomes a wave on the board \u2014 so new sprints show up automatically, no rebuild. Change it to your own convention (e.g. `Sprint`, `Wave`). The display name drops a leading `ATR `."
        },
        "atrium.linear.pollSeconds": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "markdownDescription": "Auto-refresh the cockpit from Linear every N seconds. **0 = off (default).** Only meaningful with a live API key set \u2014 try 60\u2013300 for a gentle live sync."
        }
      }
    }
  },
  "scripts": {
    "build": "vite build && node esbuild.host.mjs && cp src/atrium-board.json dist/",
    "build:webview": "vite build",
    "build:host": "node esbuild.host.mjs && cp src/atrium-board.json dist/",
    "watch:webview": "vite build --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.webview.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "vscode:prepublish": "vite build && node esbuild.host.mjs && cp src/atrium-board.json dist/"
  },
  "dependencies": {
    "@linear/sdk": "^86.0.0",
    "marked": "^18.0.5",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^20",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@types/vscode": "^1.98.0",
    "@vitejs/plugin-react": "^4.6.0",
    "@vscode/codicons": "^0.0.36",
    "autoprefixer": "^10",
    "esbuild": "^0.28.0",
    "jsdom": "^26",
    "postcss": "^8",
    "tailwindcss": "^3.4",
    "typescript": "~5.8.3",
    "vite": "^7.0.4",
    "vitest": "^4.1.5"
  }
}
